target/
node_modules/
shot.png
hit-*.png
.DS_Store
config.toml
# ...except cargo's, which carries the signing runner and has to be shared: an
# unsigned dev binary makes the Keychain re-prompt after every build.
!src-tauri/.cargo/config.toml
.env
.env.*
dist/
.signing-identity
# The certificate itself. Public, but it belongs beside the key it pairs
# with rather than in the history of the app it signs.
developerID_application.cer

# Other people's source, kept locally for reference and never shipped.
clicky/
opencode/
.signing-identity-release
.signing/
openworker/
node_modules/
.next/
web/node_modules/
web/.next/
web/.env
server/.venv/
# Anywhere, not just `server/` -- `scripts/` grew one too, and a path-pinned
# rule means the next directory to run Python sits untracked in every status.
__pycache__/
server/nudge.db
server/releases/
server/.env
web/.next/
server/.pytest_cache/
OpenExecutive/
