target/
node_modules/
shot.png
hit-*.png
.DS_Store
config.toml
.env
.env.*
dist/
.signing-identity

# Other people's source, kept locally for reference and never shipped.
clicky/
opencode/
.signing-identity-release
.signing/
openworker/
node_modules/
.next/
web/node_modules/
web/.next/
web/.env
server/.venv/
# Anywhere, not just `server/` -- `scripts/` grew one too, and a path-pinned
# rule means the next directory to run Python sits untracked in every status.
__pycache__/
server/nudge.db
server/releases/
server/.env
web/.next/
server/.pytest_cache/
OpenExecutive/
