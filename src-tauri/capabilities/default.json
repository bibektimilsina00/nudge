{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay: draw, toggle click-through. Panel: settings dropdown.",
  "windows": [
    "overlay",
    "panel"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-ignore-cursor-events",
    "core:window:allow-set-focus",
    "global-shortcut:default"
  ]
}
